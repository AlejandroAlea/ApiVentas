import { Query } from "mongoose"
import { getItem } from "../controllers/items"
import { Prenda } from "../intarfaces/prenda.interface"
import ItemModel from "../models/items.model"





const sale = async (idPrenda: string, idVendedor: string) => {
    const a = []
    a = await 
    a.forEach(idPrenda => {

        
    });





    //const listProductos = await ItemModel.findById(idPrenda);

}